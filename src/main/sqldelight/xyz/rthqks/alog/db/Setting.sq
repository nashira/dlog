CREATE TABLE Setting (
    id INTEGER PRIMARY KEY NOT NULL,
    key TEXT NOT NULL,
    type TEXT NOT NULL,
    json TEXT NOT NULL
);

selectAll:
SELECT * FROM Setting;

insert:
INSERT INTO Setting (key, type, json) VALUES ?;
